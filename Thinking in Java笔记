(2019-11-26)
 *比较两个对象的实际内容是否相同，用equals()方法，比较基本类型值是否相同，用==。
 *在Java中，String 、Math、还有Integer、Double。。。。等这些封装类重写了Object中的equals()方法，让它不再比较其对象在内存中的地址，
  而是比较对象中实际包含的整数的值，即比较的是内容。
  而我们正常创建的对象，如果没有重新equals()方法，那么它们比较的是对象在内存中的地址。
  
(2019-11-28)
  *sql:可能存在：join
       不存在：  not in

(2019-11-29)
  *sql:如果有Group By语句，则无法与where一起使用，需使用Having语句
  
(2019-11-29)
  *除构造器外，编译器禁止在其他任何方法中调用构造器。

(2019-12-01)
*GC几种使用技术
  *引用记数 特点：简单但速度很慢、无法检测对象间循环引用
  *自适应的 分代的 停止-复制 标记清扫
  
(2019-12-02)
*java堆与栈
  *1.栈内存存储的是局部变量而堆内存存储的是实体；
   2.栈内存的更新速度要快于堆内存，因为局部变量的生命周期很短；
   3.栈内存存放的变量生命周期一旦结束就会被释放，而堆内存存放的实体会被垃圾回收机制不定时的回收。
   url:https://blog.csdn.net/jianghao233/article/details/82777789
   
(2019-12-06)
*继承：B继承A, A中的某方法f()是private，B中也有该方法f(),那么B向上转型为A时，是不能调用A中的f（）.
*继承：B继承A, 运行代码时，A中static成员先初始化，其次B中static成员初始化，如果实例化B,则先调用A的构造方法（调用构造方法前，先初始化方法前的变量），
      然后调用B的构造方法（调用构造方法前，先初始化方法前的变量）。
